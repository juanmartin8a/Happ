mutation SignUp($input: SignUpInput!) {
  signUp(
    input: $input
  ) {
    user {
      id
      name
      username
    }
    tokens {
      accessToken
      refreshToken
    }
    errors {
      field
      message
    }
  }
}