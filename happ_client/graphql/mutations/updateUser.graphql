mutation UpdateUser($input: UpdateUserInput!) {
  updateUser(
    input: $input
  ) {
    user {
      id
      username
      name
      profilePic
    }
    errors {
      field
      message
    }
  }
}